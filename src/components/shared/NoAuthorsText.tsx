import { Typography } from "@mui/material";

// text to show that there are no authors
export default function NoAuthorsText() {
  return <Typography sx={{ m: 1 }}>There are no authors</Typography>;
}
